navigate to where your python installion is installed in commmand prompt
then 
python3 -m pip install -U pygame==2.6.0
or python -m pip install -U pygame==2.6.0
or python.exe -m pip install -U pygame==2.6.0
Download and run Sudoku.py
